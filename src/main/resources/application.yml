spring:
  datasource:
    url: jdbc:mysql://localhost:3307/currency_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379